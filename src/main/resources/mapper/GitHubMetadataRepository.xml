<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.regyl.gfi.repository.GitHubMetadataRepository">

    <resultMap id="GitHubMetadataEntityResultMap" type="com.github.regyl.gfi.entity.GitHubMetadataEntity">
        <id property="id" column="id"/>
        <result property="created" column="created"/>
        <result property="label" column="label"/>
        <result property="issueCount" column="issue_count"/>
        <result property="year" column="year"/>
    </resultMap>

    <insert id="save">
        INSERT INTO gfi.e_github_metadata (label, issue_count)
        VALUES (#{entity.label}, #{entity.issueCount})
        ON CONFLICT (label) DO UPDATE SET
            issue_count = EXCLUDED.issue_count,
            created = NOW()
    </insert>

    <insert id="saveAll">
        INSERT INTO gfi.e_github_metadata (label, issue_count)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.label}, #{entity.issueCount})
        </foreach>
        ON CONFLICT (label) DO UPDATE SET
            issue_count = EXCLUDED.issue_count,
            created = NOW()
    </insert>

    <select id="findAll" resultMap="GitHubMetadataEntityResultMap">
        SELECT
            id,
            created,
            label,
            issue_count
        FROM gfi.e_github_metadata
        ORDER BY issue_count DESC
    </select>

</mapper>
